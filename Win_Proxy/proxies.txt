# ==================== proxies.txt — 1 dòng = 1 IP = 1 "máy Windows" ====================
# Format:
#   protocol://[user:pass@]host:port
# Hỗ trợ: http://  https://  socks4://  socks5://
#
# TÙY CHỌN — ghi hint geo để danh tính (timezone/locale/OEM) KHỚP CHÍNH XÁC proxy:
#   protocol://user:pass@host:port #CC:City:Timezone
#   vd: socks5://user:pass@185.1.2.3:1080 #TR:Istanbul:Europe/Istanbul
# Nếu không ghi hint, script tự tra geo của IP proxy (host-side, direct).
#
# KHÔNG ghi IP VPS / IP giả vào đây — mỗi dòng PHẢI là 1 proxy residential thật.
# Mỗi proxy chỉ nên xuất hiện 1 lần (trùng lặp sẽ bị bỏ qua tự động).
#
# Ví dụ (bỏ # để dùng):
#socks5://user:pass@185.1.2.3:1080 #TR:Istanbul:Europe/Istanbul
#http://user:pass@23.94.10.10:8000 #US:New_York:America/New_York
#socks5://user:pass@88.99.1.1:1080 #GB:London:Europe/London
